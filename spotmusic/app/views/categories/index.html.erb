<section class="section has-text-white has-text-centered">
  <h2 class="is-size-3 has-text-weight-bold"><%= @category.name %></h2>
  <br />
  
  <%= render "shared/tabs", albums: @category.album, artists: @category.artist, songs: @category.song %>
</section>